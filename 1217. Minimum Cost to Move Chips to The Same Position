class Solution {
    public int minCostToMoveChips(int[] position) {
        int odd=0;
        int even=0;
        for(int a :position){
            if(a%2==0) even++;
            else odd++;
        }
        return Math.min(odd,even);
    }
}
