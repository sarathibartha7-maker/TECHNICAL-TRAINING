class Solution {
    public int[] getConcatenation(int[] nums) {
        int n=nums.length;
        int[] line = new int[2 * n];
        for (int i=0;i<n;i++){
            line[i]=nums[i];
            line[i + n]=nums[i];

        }
        return line;
    }
}
